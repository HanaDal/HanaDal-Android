<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hanadal.dooson.hanadal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".TrendingActivity" />
        <activity android:name=".AppInfoActivity" />
        <activity android:name=".QnaActivtiy" />
        <activity android:name=".MyChallengeListActivity" />
        <activity android:name=".MakeMyChallengeActivity" />
        <activity android:name=".ViewChallengeActivity" />
        <activity android:name=".WriteMyDiaryActivity" />
        <activity android:name=".ChallengeInfoActivity" />
        <activity android:name=".RequestActivity" />
        <activity android:name=".ViewBookActivity" />
        <activity android:name=".StoreActivity" />
        <activity android:name=".ChangeSkinActivity"></activity>
    </application>

</manifest>